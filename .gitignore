# Env files (never commit real secrets/keys — .env.example is the template)
**/.env

# Python (backend)
**/.venv
**/__pycache__
**/*.pyc
**/.pytest_cache

# Node (frontend)
**/node_modules
frontend/dist
frontend/*.tsbuildinfo

# Editors / OS
**/.idea
.vscode
.DS_Store
.claude/settings.local.json
